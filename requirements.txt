# ============================================================================
# requirements.txt — curated DIRECT dependencies, fully pinned.
# Python 3.11. One-shot install (run FROM THE REPO ROOT so the relative
# editable paths resolve):
#
#     python3.11 -m venv venv11 && source venv11/bin/activate
#     pip install -r requirements.txt
#
# pip resolves and adds the transitive closure itself. For a byte-exact clone
# of the current working venv (including its historical leftovers) use
# requirements.lock.txt instead (see its header).
# ============================================================================

# --- numerics (numpy MUST stay <2: scipy 1.11 / matplotlib 3.8 wheels in this
#     stack are built against the NumPy 1.x ABI; NumPy 2 breaks them)
numpy==1.26.4
scipy==1.11.3
pandas==2.1.1
matplotlib==3.8.0

# --- core PDDL / planning stack
pddl==0.4.3
pddl-plus-parser==3.14.7
anytree==2.8.0
gym==0.26.2

# --- vision / image handling
opencv-python==4.10.0.84
Pillow==9.5.0
imageio==2.36.1  # pddlgym frame rendering

# --- LLM backends (fluent classification / object detection)
openai==2.9.0
google-genai==1.53.0

# --- ROSAME baselines (torch stack)
torch==2.9.1
torchvision==0.24.1  # matching pair for torch 2.9.1 (venv11 historically held
                     # 0.16.2, which declares torch==2.1.2 — a fresh resolve
                     # would refuse that pair, so it is aligned here)

# --- ROSAME+MILP baseline (CP-SAT solution fixer)
cpmpy==1.0.0        # constraint-model layer
ortools==9.14.6206  # CP-SAT backend; 9.14 is the last line compatible with
                    # numpy<2 (9.15+ drags numpy 2.x in)
protobuf==6.31.1    # ortools 9.15 upgrades it; 6.31.1 matches the rest
# gurobipy          # optional: uncomment to enable the "mip-gurobi" encoder
                    # (requires a Gurobi license)

# --- evaluation stack (AMLGym metrics dependencies)
tarski==0.9.1
clingo==5.8.0             # required by tarski's LPGroundingStrategy
unified-planning==1.3.0
up-fast-downward==0.5.2   # planner backend for solving metrics
plotly==6.3.0             # interactive precision/recall plots

# --- misc
PyYAML==6.0.2
XlsxWriter==3.2.5  # Excel experiment reports
pytest==7.4.2      # dev/tests

# --- local editable checkouts (siblings of this repo in ~/Documents/BGU/thesis)
# On a fresh machine, clone/copy these BEFORE installing (pinned commits
# recorded from the working venv, 2026-07-20):
#   git clone https://github.com/tomsilver/pddlgym.git         ../pddlgym           && git -C ../pddlgym           checkout ab268e40ac7b2de8d060ec0c48c27003d1a62d62
#   git clone https://github.com/ronuchit/pddlgym_planners.git ../pddlgym_planners  && git -C ../pddlgym_planners  checkout 060d1dd632df5f101d36493116d5377cc2f48fbd
#   git clone https://github.com/LamannaLeonardo/AMLGym.git    ../AMLGym            && git -C ../AMLGym            checkout e9d8615988ae139cab7c5e1def3184ddccb49a90
# sam_learning: commit ad7ca64eeb2 exists ONLY in the local checkout (NOT on
# GitHub — local commits were never pushed). Copy ~/Documents/BGU/thesis/
# sam_learning from the current machine; do not clone from upstream.
-e ../pddlgym
-e ../pddlgym_planners
-e ../sam_learning
-e ../AMLGym
