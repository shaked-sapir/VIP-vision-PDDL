(define (domain hanoi)
(:requirements :equality :negative-preconditions :typing :strips)
(:types 	peg disc - object
)

(:predicates (clear-peg ?x - peg)
	(clear-disc ?x - disc)
	(on-disc ?x - disc ?y - disc)
	(on-peg ?x - disc ?y - peg)
	(smaller-disc ?x - disc ?y - disc)
	(smaller-peg ?x - peg ?y - disc)
)

(:action move_disc_disc
	:parameters (?disc - disc ?from - disc ?to - disc)
	:precondition (and (smaller-disc ?disc ?from)
	(smaller-disc ?disc ?to)
	(smaller-disc ?from ?disc)
	(smaller-disc ?from ?to)
	(smaller-disc ?to ?disc)
	(smaller-disc ?to ?from))
	:effect (and (clear-disc ?disc)
		(clear-disc ?from)
		(clear-disc ?to)
		(not (clear-disc ?disc))
		(not (clear-disc ?from))
		(not (clear-disc ?to))
		(not (on-disc ?disc ?from))
		(not (on-disc ?disc ?to))
		(on-disc ?disc ?from)
		(on-disc ?disc ?to) 
		))

(:action move_disc_peg
	:parameters (?disc - disc ?from - disc ?to - peg)
	:precondition (and (clear-peg ?to)
	(on-disc ?disc ?from)
	(smaller-disc ?from ?disc)
	(smaller-peg ?to ?disc)
	(smaller-peg ?to ?from))
	:effect (and (clear-disc ?from)
		(not (clear-disc ?disc))
		(not (clear-disc ?from))
		(not (clear-peg ?to))
		(not (on-disc ?disc ?from))
		(on-peg ?disc ?to) 
		))

(:action move_peg_disc
	:parameters (?disc - disc ?from - peg ?to - disc)
	:precondition (and (clear-disc ?disc)
	(smaller-disc ?disc ?to)
	(smaller-disc ?to ?disc)
	(smaller-peg ?from ?disc)
	(smaller-peg ?from ?to))
	:effect (and (clear-peg ?from)
		(not (clear-disc ?to))
		(on-disc ?disc ?to) 
		))

)