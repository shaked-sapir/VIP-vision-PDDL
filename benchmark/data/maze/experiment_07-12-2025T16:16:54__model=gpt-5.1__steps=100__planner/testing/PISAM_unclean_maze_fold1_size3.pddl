(define (domain maze)
(:requirements :equality :strips :negative-preconditions :typing)
(:types 	player location - object
)

(:predicates (move-dir-up ?v0 - location ?v1 - location)
	(move-dir-down ?v0 - location ?v1 - location)
	(move-dir-left ?v0 - location ?v1 - location)
	(move-dir-right ?v0 - location ?v1 - location)
	(clear ?v0 - location)
	(at ?v0 - player ?v1 - location)
	(oriented-up ?v0 - player)
	(oriented-down ?v0 - player)
	(oriented-left ?v0 - player)
	(oriented-right ?v0 - player)
	(is-goal ?v0 - location)
)

(:action move_up
	:parameters (?p - player ?from - location ?to - location)
	:precondition (and (at ?p ?from)
	(clear ?to)
	(move-dir-down ?to ?from)
	(move-dir-up ?from ?to))
	:effect (and (at ?p ?to)
		(clear ?from)
		(not (at ?p ?from))
		(not (clear ?to))
		(not (oriented-left ?p))
		(oriented-up ?p) 
		))

(:action move_down
	:parameters (?p - player ?from - location ?to - location)
	:precondition (and (at ?p ?from))
	:effect (and (at ?p ?to)
		(clear ?from)
		(move-dir-down ?from ?to)
		(move-dir-up ?to ?from)
		(not (at ?p ?from))
		(not (clear ?to))
		(not (oriented-left ?p))
		(oriented-down ?p) 
		))

(:action move_left
	:parameters (?p - player ?from - location ?to - location)
	:precondition (and (at ?p ?from)
	(clear ?to)
	(move-dir-left ?from ?to)
	(move-dir-right ?to ?from))
	:effect (and (at ?p ?to)
		(clear ?from)
		(not (at ?p ?from))
		(not (clear ?to))
		(not (oriented-right ?p))
		(oriented-left ?p) 
		))

(:action move_right
	:parameters (?p - player ?from - location ?to - location)
	:precondition (and (at ?p ?from)
	(clear ?to)
	(move-dir-left ?to ?from)
	(move-dir-right ?from ?to))
	:effect (and (at ?p ?to)
		(clear ?from)
		(not (at ?p ?from))
		(not (clear ?to))
		(not (oriented-up ?p))
		(oriented-right ?p) 
		))

)