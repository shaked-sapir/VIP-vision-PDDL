# Future Tasks

| Task name | Description | What exists now | What we should do |
|---|---|---|---|
| Wire `inference_mode=True` for LLM object detection | Enable end-to-end visual object detection in the main trajectory pipeline instead of reading object lists from GT trajectory JSON. | `LLMObjectDetector` supports `inference_mode=True` and sends a 1-shot reference image + labels to the LLM, but `LLMVisualComponentsMixin.init_visual_components` always creates detectors with the default `inference_mode=False`, so `detect()` returns GT objects from `fewshot_examples[0][1]` without calling the LLM. Only component tests (e.g. depot/gripper) pass `inference_mode=True`. | Pass `inference_mode=True` from `LLMVisualComponentsMixin` (or make it configurable via handler init / `config.yaml`). Verify object lists are stable across frames, update prompts if needed, and compare pipeline quality vs the current GT-object shortcut. |
| ROSAME-I: ImageNet-pretrained backbone variant | The ROSAME-I baseline (see `docs/rosame-i-implementation-plan.md`) ships with a random-init ResNet-18, faithful to the ICAPS-24 paper. At our data scale (3–8 trajectories ≈ 30–100 images vs their 70–2,500 traces) a pretrained backbone would likely help substantially. | Plan specifies random init as the only variant (settled decision). | Add a `pretrained=True` labeled variant (e.g. algorithm name `ROSAME-I (pretrained)`), run side-by-side on the imaged experiments, and report both — clearly marking the pretrained one as a deviation from the published method. |
| ROSAME-I: sigmoid-activation CV head variant | ROSAME-I ships a raw-logit CV head, faithful to the ICAPS-24 synth/resnet path (chosen for a fair comparison — minimal deviation from the published code). The paper's `CVGrid` path instead applies a sigmoid, giving CV outputs proper [0,1] proposition semantics that may stabilise the consistency/validity losses at our tiny data scale. | `RosameI_Runner._trajectory_loss` uses `cv_model(images)` logits directly; `train_per_trajectory`/pooled loops both implemented. | Add a `sigmoid=True` labeled variant (e.g. `ROSAME-I (sigmoid)`), run side-by-side on the imaged experiments, and report both — marking which matches the paper's synth vs CVGrid path. |
| Decide how to display baselines (ROSAME) against CDPS in dashboard + paper | CDPS returns a *set* of CFMs (anytime), baselines return a single model — the display must not compare them on an unfair axis. Options discussed: (A) dashed hline at the baseline's across-folds value over the existing CFM-index trend plots — cheap but axis-unfair (CFM index is CDPS-internal; ROSAME's full budget vs each CFM's partial budget) and it mixes the internal "later CFMs are better" claim with the external comparison claim; (B) time-axis anytime plot — x = wall-clock, CDPS = best-so-far-by-cost step curve of the *returned* model, ROSAME = point at training-completion time extended rightward; resource-honest, data already exists (`wall_time_so_far` in `conflict_free_solutions_log`, `learning_time_seconds` for baselines); needs hardware caveat (GPU train vs CPU search); (C) final-vs-final — designated CDPS output (min-cost CFM at full budget) vs baseline per metric with CI; the primary reviewer-facing comparison; (D) distribution view — box/violin of the CFM set per cell with baseline as marker; native to the set-of-hypotheses story, no ordering axis; (E) crossing-point heatmap over the mask×noise grid — budget needed to match the baseline. | Baseline rows land in `fold_result.json` (same schema, `algorithm` field); trend plots (`build_dashboard.py`) are CDPS-only; `_plot_trend_thumb` has no baseline overlay. | Decide the primary display (leaning: C primary, B for the anytime story, existing CFM-index trend stays baseline-free, D optional, A dashboard-only if at all). Open sub-decisions: selection rule for "the returned model" (min-cost vs latest on cost ties); whether B's clock includes evaluation overhead or pure search; whether ROSAME's point in B uses actual or normalized training time. Then implement in `build_dashboard.py`. |
