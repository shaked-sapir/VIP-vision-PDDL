(define (domain blocks)
(:requirements :strips :negative-preconditions :typing :equality)
(:types 	block robot - object
)

(:predicates (on ?x - block ?y - block)
	(ontable ?x - block)
	(clear ?x - block)
	(handempty ?x - robot)
	(handfull ?x - robot)
	(holding ?x - block)
)

(:action pick-up
	:parameters (?x - block ?robot - robot)
	:precondition (and (handempty ?robot)
	(not (handfull ?robot))
	(not (holding ?x)))
	:effect (and (handfull ?robot)
		(holding ?x)
		(not (clear ?x))
		(not (handempty ?robot))
		(not (ontable ?x)) 
		))

(:action put-down
	:parameters (?x - block ?robot - robot)
	:precondition (and )
	:effect (and (clear ?x)
		(handempty ?robot)
		(not (handfull ?robot))
		(not (holding ?x))
		(ontable ?x) 
		))

(:action stack
	:parameters (?x - block ?y - block ?robot - robot)
	:precondition (and (not (holding ?y))
	(not (on ?x ?y))
	(not (on ?y ?x))(not (= ?x ?y)))
	:effect (and (clear ?x)
		(handempty ?robot)
		(not (clear ?y))
		(not (handfull ?robot))
		(not (holding ?x))
		(on ?x ?y) 
		))

(:action unstack
	:parameters (?x - block ?y - block ?robot - robot)
	:precondition (and (handempty ?robot)
	(not (handfull ?robot))
	(not (holding ?x))
	(not (holding ?y))
	(not (on ?y ?x))
	(not (ontable ?x))(not (= ?x ?y)))
	:effect (and (clear ?y)
		(handfull ?robot)
		(holding ?x)
		(not (clear ?x))
		(not (handempty ?robot))
		(not (on ?x ?y)) 
		))

)