from src.types import StateRepresentation


class Image(StateRepresentation):
    def __init__(self):
        #TODO: this class should be defined by the return value of cv.read(), as this is the image we are going to work with for now
        pass